-- The ℝ layer: proofs only, never executed, never emitted into an Artifact.
-- Kept in a separate Lake package so AlgorithmLib stays Mathlib-free.
import AlgorithmLibReal.Carrier
import AlgorithmLibReal.SDeriv
