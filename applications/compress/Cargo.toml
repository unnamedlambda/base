[package]
name = "compress"
version = "0.1.0"
edition = "2021"

[dependencies]
base = { path = "../../base" }
bincode = "1"

[build-dependencies]
build-support = { path = "../../build-support" }

[dev-dependencies]
tempfile = "3"

[[bin]]
name = "compress"
path = "src/main.rs"
